[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Перенаправляем все запросы к /api/* на нашу функцию
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api-proxy/:splat"
  status = 200